#! /usr/bin/env bash
set -o errexit
set -o nounset
set -o pipefail

response=$(echo -e "Yes\nNo" | rofi -lines 2 -dmenu -p "Exit i3?")
case $response in
    Yes)
        i3-msg exit
        ;;
    No)
        ;;
esac
