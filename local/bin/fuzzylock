#! /usr/bin/env bash
# Take a screen shot, pixelate it, and use it as the lock screen.
screensize=$(xrandr | grep -F '*' | mawk -F' ' '{print($1)}')
scrot --silent -F - |
    convert - -scale 12.5% -scale 800% RGB:- |
    i3lock -e --raw "${screensize}:rgb" --image /dev/stdin
