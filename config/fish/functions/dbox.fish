function dboxe --description 'distrobox enter'
    distrobox enter $argv
end

function dboxls --description 'distrobox list'
    distrobox list $argv
end

function dboxs --description 'distrobox stop'
    distrobox stop $argv
end
